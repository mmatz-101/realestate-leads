name: Build and Release

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Binaries
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'

      - name: Get commit info
        id: commit
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

      - name: Build for Linux (amd64)
        run: |
          GOOS=linux GOARCH=amd64 go build -o realestate-leads-linux-amd64 ./cmd/main.go

      - name: Build for Linux (arm64)
        run: |
          GOOS=linux GOARCH=arm64 go build -o realestate-leads-linux-arm64 ./cmd/main.go

      - name: Build for macOS (amd64)
        run: |
          GOOS=darwin GOARCH=amd64 go build -o realestate-leads-macos-amd64 ./cmd/main.go

      - name: Build for macOS (arm64)
        run: |
          GOOS=darwin GOARCH=arm64 go build -o realestate-leads-macos-arm64 ./cmd/main.go

      - name: Build for Windows (amd64)
        run: |
          GOOS=windows GOARCH=amd64 go build -o realestate-leads-windows-amd64.exe ./cmd/main.go

      - name: Generate checksums
        run: |
          sha256sum realestate-leads-* > checksums.txt
          cat checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ steps.commit.outputs.date }}-${{ steps.commit.outputs.sha_short }}
          name: Build ${{ steps.commit.outputs.date }}-${{ steps.commit.outputs.sha_short }}
          body: |
            Automated build from commit ${{ github.sha }}

            ## Download Instructions

            1. Download the binary for your platform
            2. (Optional) Set the `TX_COMPTROLLER_API_KEY` environment variable for Preview/Full modes
            3. Make it executable (Linux/macOS): `chmod +x realestate-leads-*`
            4. Run: `./realestate-leads-<platform>`
            5. Browser will open automatically to http://localhost:8080

            ## Features

            - **Web UI**: Modern browser-based interface
            - **Three Processing Modes**:
              - Forewarn Only (default)
              - Preview Mode (Comptroller lookup only)
              - Full Pipeline (Comptroller → Forewarn)
            - **Real-time Progress**: Live updates with SSE
            - **Multi-stage Downloads**: Access both stage 1 and stage 2 results

            ## Checksums
            ```
            $(cat checksums.txt)
            ```
          files: |
            realestate-leads-linux-amd64
            realestate-leads-linux-arm64
            realestate-leads-macos-amd64
            realestate-leads-macos-arm64
            realestate-leads-windows-amd64.exe
            checksums.txt
          draft: false
          prerelease: false
